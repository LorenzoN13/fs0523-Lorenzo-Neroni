import { ColoraTestoDirective } from './colora-testo.directive';

describe('ColoraTestoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoraTestoDirective();
    expect(directive).toBeTruthy();
  });
});
